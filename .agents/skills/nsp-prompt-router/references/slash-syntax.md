# /nsp Slash Syntax

Canonical:

```text
/nsp <verb> <free text args>
```

Supported aliases:

```text
/nsp:<verb>:<args>
@nsp <verb> <args>
use NSP to <intent>
route this through NSP
ask NSP <question>
review this with NSP
```

Normalize aliases to `_nsp <verb> --target .`.
